import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#7c3aed"
        },
        secondary: {
            main: "#0ea5e9"
        },
        background: {
            default: "#EDE9FE"
        }
    }
})

export default theme;